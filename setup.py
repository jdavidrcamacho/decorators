from setuptools import find_packages, setup

setup(
    name='decorators',
    packages=find_packages(),
    version='0.1.0',
    description='Just a decorator thing',
    author='Camacho',
    license='',
    # packages=['decorators'],
    install_requires=[
        'numpy', 
      ],
)
